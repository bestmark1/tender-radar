# Cross-Artifact Analysis — Тендер-радар

> Роль: Analyzer. Проверка согласованности Narrative/MRD/PRD/Architecture/Plan: противоречия, пропущенные зависимости, риски порядка фаз, критерии приёмки без пути реализации, архитектурные решения без пути валидации.

## Статус: PASS (блокеров нет; 4 minor-замечания с решениями на этапе исполнения)

## 1. Трассировка критериев приёмки → реализация

| User Story | AC | Путь реализации | Валидация | OK |
|---|---|---|---|---|
| US-1 радары | 1.1–1.4 | Фаза 1 (radars + CRUD) | npm test + ручная | ✅ |
| US-2 загрузка | 2.1–2.5 | Фаза 2 (скелет) → Фаза 3 (реал) | прогон WF1 + SQL | ✅ |
| US-3 фильтр | 3.1–3.4 | Фаза 4 (WF2) | прогон + SQL | ✅ |
| US-4 уведомления | 4.1–4.5 | Фаза 5 (WF3/WF4) | тест-рассылка + notifications | ✅ |
| US-5 дашборд | 5.1–5.5 | Фаза 6 | npm test + ручная | ✅ |
| US-6 защита | 6.1–6.5 | Фаза 2 (часть) + Фаза 7 | curl + git grep | ✅ |

Все критерии приёмки имеют путь реализации и валидации.

## 2. Архитектурные решения → путь валидации

| ADR | Валидируется | OK |
|---|---|---|
| ADR-1 две БД (Supabase бизнес, N8N служебная) | Фаза 2: запись в Supabase, не в БД N8N | ✅ |
| ADR-2 фронт пишет радары в Supabase | Фаза 1 | ✅ |
| ADR-3 загрузка по региону, фильтр у нас | Фаза 3/4 | ✅ |
| ADR-6 queue mode + Redis | Фаза 0/2 (worker-контейнер) | ✅ |

## 3. Minor-замечания (исправить в ходе исполнения, не блокируют)

| # | Замечание | Где | Решение |
|---|---|---|---|
| A1 | Несогласованность: обзорная таблица плана для Фазы 2 указывает «зависит от 0», а тело фазы и registry — «0 и 1» (нужна таблица `tenders` из Фазы 1) | IMPLEMENTATION_PLAN обзор vs тело | Истина — «0 и 1». Учесть при исполнении Фазы 2 (Фаза 1 идёт раньше). |
| A2 | Не определено, кто создаёт строку в `users` при регистрации (Supabase Auth создаёт `auth.users`, но не нашу `users`) | ARCHITECTURE §4, Фаза 1 | В Фазе 1 добавить триггер/insert при первом входе (on auth signup → upsert users). |
| A3 | Таблица `tenders` не имеет `user_id` (глобальные данные), а RLS-правило сформулировано «все таблицы с user_id» | ARCHITECTURE §4 | В Фазе 1: для `tenders` RLS = read для authenticated, write только service_role. Явно прописать в миграции. |
| A4 | Фаза 6 (дашборд) для полноценного теста требует реальных `matches` (Фаза 4 → токен), хотя в registry помечена «токен не нужен» | phase-registry | Дашборд верстается без токена на мок-данных Фазы 2; полная сквозная проверка — после токена. Уточнить в registry. |

## 4. Порядок фаз / зависимости

- Критический путь `0→1→2→3→4→5→7` корректен. Фаза 6 параллельна 5 (обе после 4).
- Единственный внешний блокер — токен ЕИС (Задача 0) перед Фазой 3. Фазы 0–2 от него не зависят. ✅
- Циклических зависимостей нет.

## 5. Противоречия между артефактами

- Narrative/PRD/MRD по каналам, региону, вертикали, скоупу 44-ФЗ — согласованы (после правок: клининг 81.2, Москва+МО, email/VK/MAX/Telegram).
- MAX: PRD AC-4.1 + риск R8 + tech-debt TD-5 + Фаза 5 fallback — согласованы.
- Противоречий, блокирующих исполнение, не найдено.

## Вывод

Готово к исполнению. Замечания A1–A4 — операционные, устраняются в соответствующих фазах (преимущественно Фаза 1). ANALYZE-гейт: **PASS**.
